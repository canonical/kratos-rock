name: auto-approver
run-name: CI for approving PR 

on:
  push:
    branches:
      - "renovate/**"

jobs:
  autoapprove:
    uses: canonical/identity-team/.github/workflows/pr-auto-approval.yaml@e017941885bd1d17774bf7be2cbbb1d1abe80d9e # v1.16.0
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}