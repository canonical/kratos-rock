name: auto-approver
run-name: CI for approving PR 

on:
  push:
    branches:
      - "renovate/**"

jobs:
  autoapprove:
    uses: canonical/identity-team/.github/workflows/pr-auto-approval.yaml@e722462193a22c5126d622ebfe9916d83b415d6d # v1.12.0
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}