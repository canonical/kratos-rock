name: auto-approver
run-name: CI for approving PR 

on:
  push:
    branches:
      - "renovate/**"

jobs:
  autoapprove:
    uses: canonical/identity-team/.github/workflows/pr-auto-approval.yaml@b5070d98993fa72f429f4fdf8010f6f9a94eb4e0 # v1.14.2
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}